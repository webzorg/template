# # quickly generate new project without js and bundling for speed
# rm -rf test_rails_app; rails new test_rails_app --skip-spring --skip-test --database=postgresql --webpacker=stimulus --template=~/www/template/template.rb
# rm -rf test_rails_app; rails new test_rails_app -J -B; ls -alh test_rails_app

# # references
# https://github.com/TanookiLabs/tanooki-rails-template/blob/master/rails-kickoff-template.rb
# https://github.com/excid3/jumpstart/blob/master/template.rb
# https://github.com/RailsApps/rails-composer/blob/master/composer.rb

# # ~/.railsrc
#--skip-bundle
#--skip-spring
#--skip-test
#--database=postgresql
#--webpacker=stimulus
#--template=~/www/template/template.rb

# cloud init status report
cat /var/lib/cloud/instance/boot-finished
cat /var/lib/cloud/data/result.json

####################

# Used as the base secret for all MessageVerifiers in Rails, including the one protecting cookies.
secret_key_base:

sendgrid_key:
# sms_office_key:

admin_mail: "labulashvili@gmail.com"
info_mail: "labulashvili@gmail.com"

aws:
  access_key_id:
  secret_access_key:
  staging:
    bucket_name:
  production:
    bucket_name:
  region: eu-central-1

facebook:
  app_id:
  app_secret:

google:
  analytics_code:
  # api_key:
  client_id:
  client_secret:

development:
  host: "localhost:3000"
  # db_url: "postgres://user:pass@host:5432/app_development"

staging:
  host: "dev.wishmaster.ge"
  # db_url: "postgres://user:pass@host:5432/app_development"

production:
  host: "wishmaster.ge"
  # db_url: "postgres://user:pass@host:5432/app_development"
